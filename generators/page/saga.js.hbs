import { call, put, takeLatest } from 'redux-saga/effects';

import {
    {{lowerCase name}}Routine,
} from './actions';

export function* doDefault(action) {
    const response = yield call(fetch, 'https://jsonplaceholder.typicode.com/comments?postId=1');
    const comments = yield response.json();

    yield put({{lowerCase name}}Routine.success(comments));

    return comments;
}

export default function* {{lowerCase name}}Saga() {
    yield takeLatest({{lowerCase name}}Routine.TRIGGER, doDefault);
}
